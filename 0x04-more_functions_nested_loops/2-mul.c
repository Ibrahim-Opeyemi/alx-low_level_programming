#include "main.h"
#include <stdio.h>

/**
 * mul - Function to multiply two integers.
 * @a: The first integer
 * @b: The second integer
 *
 * Return: The product of 'a' and 'b'.
 */

int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
